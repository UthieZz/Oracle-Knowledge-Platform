# Audit 2026-09-17 — Oracle Knowledge Platform

Repo: `UthieZz/Oracle-Knowledge-Platform` (user said Orackle; actual name is Oracle).
Constraint: no edits under `okc/`.

## Verdict

Dual stack is implementation truth: `okc/` IR/compiler surface plus live `src/` + `studio/` product path.
Provenance repair exists. Publish through ExportService already fail-closes when Firestore is in the exporter set.
Direct `FirestoreExporter.export()` still called repair with `strict=False`, so incomplete lineage could still land in Firestore if something bypasses ExportService.

Knowledge objects remain conversation-shaped. That is an analyzer quality problem. Do not fake compilation in Studio.

## Findings (evidence)

| Item | Class | Action this pass |
|---|---|---|
| `okc/` vs `src/` dual KnowledgeObject | architecture, locked | leave; do not merge by rewrite |
| `FirestoreExporter.export` non-strict provenance | broken publish path | fail closed (`strict=True`) |
| KO `conversation_id` field not synced from repaired provenance | weak contract | validator copies recoverable field values |
| Enricher `object_type=conversation` | quality, not a contract bug | leave; quality annotator already flags |
| Enricher `evidence` = full text after enrich | contract drift vs message-id evidence | not rewritten this pass (behavior change for retrieval) |
| `Dockerfile` | obsolete for Studio | leave; do not expand |
| Empty `docs/Plugin_System.md`, `docs/Roadmap.md`, `requirements.txt~` | redundant | not deleted without confirmation |
| Committed `__pycache__` | junk | do not treat as source |
| Flask still present | transitional | do not delete until Firestore-only Studio is verified |
| Vector DB / Cloud Run | not justified | do not add |

## This pass (outside `okc/`)

- `ensure_knowledge_object_provenance` also fills empty `KnowledgeObject.conversation_id`, `source_platform`, `source_file` from repaired provenance.
- `FirestoreExporter.export` uses `strict=True`. Incomplete lineage raises before write.
- Tests assert field sync and that Firestore export refuses an orphan KO.
- This audit record.

## Provenance contract

Required on every KnowledgeObject before Firestore publish:

- `provenance.source_platform`
- `provenance.source_file`
- `provenance.conversation_id`

Filled when recoverable: `message_ids`, `evidence`, `attachment_ids`, `object_type`, `imported_at`.
Object fields should match those keys when they exist on the dataclass.

## Do not do next

- Edit `okc/`.
- Add vector DB, Cloud Run, enterprise RBAC, or a second compiler in React.
- Delete Flask until local-first / Firestore-only Studio is verified on a real publish.
- Invent `Unmapped` in Studio to hide missing platform.
