# Audit 2026-09-19 — Oracle Knowledge Platform

Requested `Orackle-Knowledge-Platform.git` does not exist.
Audited `UthieZz/Oracle-Knowledge-Platform` @ main (`601cde9`).
Constraint: no edits under `okc/`.

## Verdict

Product path remains `src/` + `studio/`. `okc/` is a parallel locked surface, not the runtime compiler used by Studio/Firestore.

Provenance gate exists and is already wired into ExportService and FirestoreExporter (`strict=True` on publish). Remaining gap: top-level KnowledgeObject fields could drift from nested `provenance`, so Studio and portable JSON could disagree.

Knowledge quality is still conversation-shaped. That is a compiler/analyzer problem. Not patched by rewriting titles here.

## Weak / redundant (current facts)

- Dual IR: `okc/models/knowledge_package.py` vs `src/models/*`. Runtime uses `src/`.
- `okc/` Flask-ish server + Dockerfile exist. Do not expand them into the Studio runtime.
- `requirements.txt~` is leftover backup weight.
- Committed compiled output under `output/` and `output_split/` is bulky and not source-of-truth.
- Empty docs: `docs/Plugin_System.md`, `docs/Roadmap.md` (root `ROADMAP.md` is the live one).
- Services `project_service.py`, `pipeline_service.py`, `plugin_service.py`, `knowledge_service.py` are thin stubs.

## This pass (outside `okc/`)

- Provenance normalizer now copies recovered lineage onto KO top-level fields: `source_platform`, `source_file`, `conversation_id`.
- MultiSourceExporter runs the same normalizer before portable write so Markdown/JSON inherit lineage without Firestore.
- Firestore KO documents prefer top-level `conversation_id` then provenance.
- Tests assert top-level field alignment after repair.
- This audit record.
- `scripts/ko_lineage_report.py` for offline package JSON reporting.

## Provenance contract (unchanged required keys)

Required before Firestore write:
`source_platform`, `source_file`, `conversation_id`.

Filled when recoverable:
`message_ids`, `evidence`, `attachment_ids`, `object_type`, `imported_at`.

## Do not do next

- Edit `okc/`.
- Treat conversation titles as knowledge objects.
- Add vector DB / Cloud Run / second compiler in React.
- Delete Flask until a verified replacement path exists.
