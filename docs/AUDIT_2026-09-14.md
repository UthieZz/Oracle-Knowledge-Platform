# Audit 2026-09-14 — Oracle Knowledge Platform

Requested `Orackle-Knowledge-Platform.git` does not exist.
Audited `UthieZz/Oracle-Knowledge-Platform` @ main (`e5f78dd`).

Constraint honored: no edits under `okc/`.

## Verdict

Working beta: importer → KnowledgePackage → compiler → portable export + tenant/silo Firestore publish.
The first broken Studio boundary was the **read path**: exporter writes `tenants/{tid}/silos/{sid}/*`; Studio queried root collections. Rules deny those reads. Empty Studio is a contract bug, not a compiler bug.

## Weak / broken / redundant (facts)

- Dual trees: `okc/` (21 py) vs `src/` (102 py). Runtime Studio + exporters live in `src/` + `studio/`. `okc/` is the locked compiler surface.
- `run.py studio` launched `src.studio.api_server` (Flask). Flask is not the permanent runtime. Fixed to Vite.
- `ExportService()` constructed `FirestoreExporter()` at import time. Missing `OKP_TENANT_ID`/`OKP_SILO_ID` crashes portable export. Fixed: register Firestore only when both env vars exist.
- `requirements.txt~` empty backup file. Dead.
- Dockerfile exists; locked architecture says do not add Docker to solve Studio. Image is compiler-only placeholder. Leave; do not expand.
- Knowledge quality still conversation-shaped. That is a compiler/analyzer problem. Not faked in Studio.

## Provenance contract

Required after repair pass: `source_platform`, `source_file`, `conversation_id`.
Also filled when recoverable: `message_ids`, `evidence`, `attachment_ids`, `object_type`, `imported_at`.
This pass copies `conversation.source` into missing `source_file`.

## Verify

```text
python -m unittest tests.test_knowledge_object_provenance tests.test_knowledge_object_quality tests.test_export_service_lazy_firestore tests.test_firestore_exporter
```
