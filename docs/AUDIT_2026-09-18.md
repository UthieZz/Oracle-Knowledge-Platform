# Audit 2026-09-18 — Oracle Knowledge Platform

Requested `Orackle-Knowledge-Platform.git` does not exist.
Audited `UthieZz/Oracle-Knowledge-Platform` @ main.
Constraint: no edits under `okc/` and no edits to established OKC compiler/importer/analyzer passes.

## Verdict

Same dual-tree architecture as prior audits. Product path is `src/` + `studio/`.
`okc/` remains the locked compiler surface.

Knowledge quality is still conversation-shaped. That is an analyzer/compiler problem. Not faked here.

## Weak / broken / redundant (current facts)

- Dual IR: `okc/models/knowledge_package.py` (Pydantic IR v2) vs `src/models/knowledge_object.py` (dataclass). Runtime exporters use `src/`.
- FirestoreExporter repaired KO provenance but called `ensure_knowledge_object_provenance(package)` without `strict=True`. Incomplete lineage could still publish if ExportService was bypassed.
- Entity documents carried conversation/message ids but not `source_platform` / `source_file`.
- `requirements.txt~` is an empty backup. Dead weight.
- Dockerfile exists. Do not expand it into a Studio runtime.
- Committed `__pycache__` if present is dead. `.gitignore` already excludes it.

## This pass (outside `okc/`)

- FirestoreExporter: `ensure_knowledge_object_provenance(package, strict=True)` before write.
- Entity publish: copy `source_platform` and `source_file` from the parent conversation when present.
- Tests: complete KO lineage in the happy path; reject incomplete KO provenance; assert entity lineage fields.
- This audit record.

## Provenance contract

Required on KnowledgeObject before Firestore write:
`source_platform`, `source_file`, `conversation_id`.
Filled when recoverable: `message_ids`, `evidence`, `attachment_ids`, `object_type`, `imported_at`.

Entities inherit conversation `source_platform` and `source_file` at publish time.

## Do not do next

- Edit `okc/`.
- Rewrite KnowledgeObject dataclass without an explicit contract bump.
- Add vector DB, Cloud Run, or a second compiler in React.
- Delete Flask until a verified replacement path exists.
