# Audit 2026-09-20 — Oracle Knowledge Platform

Requested `Orackle-Knowledge-Platform.git` does not exist.
Audited `UthieZz/Oracle-Knowledge-Platform` @ main `601cde9` then implemented on `audit-2026-09-20`.
Constraint: no edits under `okc/`.

## Verdict

Product path remains `src/` + `studio/`. `okc/` is locked compiler surface and was not touched.
Knowledge quality is still conversation-shaped. That is an analyzer/compiler problem. Not faked here.

## Facts from repo

- Dual IR still exists: `okc/models` Pydantic vs `src/models` dataclass. Runtime exporters use `src/`.
- FirestoreExporter already called `ensure_knowledge_object_provenance(package, strict=True)` after the 2026-09-18 pass.
- ExportService previously defaulted strict only when a Firestore exporter was registered.
- KO top-level `conversation_id` / `source_platform` / `source_file` could stay empty after provenance dict repair.
- Relationship index edges omitted source identity.
- Empty `docs/Plugin_System.md`, `docs/Roadmap.md`, and `requirements.txt~` remain dead weight.
- Committed `__pycache__` directories remain dead. `.gitignore` already excludes them.
- Dockerfile exists. Do not expand it into a Studio runtime.

## This pass (outside `okc/`)

- Provenance repair now copies recovered identity onto KO fields, not only `provenance`.
- ExportService defaults to `strict_provenance=True` for portable and published destinations. Opt-out remains `strict_provenance=False`.
- Relationship index edges include `source_platform` and `source_file` when present.
- Tests updated for default-strict export and field-level identity sync.
- This audit record.

## Provenance contract

Required before export (default):
`source_platform`, `source_file`, `conversation_id` — both on `KO.provenance` and on matching KO fields when recoverable.

Filled when recoverable: `message_ids`, `evidence`, `attachment_ids`, `object_type`, `imported_at`.

Firestore remains a last-line strict gate if ExportService is bypassed.

## Do not do next

- Edit `okc/`.
- Unify dual IR without an explicit contract bump.
- Add vector DB, Cloud Run, or a second compiler in React.
- Delete Flask until a verified replacement path exists.
