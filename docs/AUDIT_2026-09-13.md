# Audit 2026-09-13 — Oracle Knowledge Platform

Requested `Orackle-Knowledge-Platform.git` does not exist.
Audited `UthieZz/Oracle-Knowledge-Platform` @ main `764bf0a`.

Constraint: did not edit OKC compiler, importers, analyzers, KnowledgeObject dataclass, or KnowledgePackage internals.

## Verdict

Compiler pipeline is real. Weakness is not missing files; it is conversation-shaped knowledge plus provenance as an untyped dict repaired after compile.
That repair boundary is correct under the no-OKC-edit constraint.

## Facts

- KO contract: id, title, content, source_platform, source_file, timestamps, provenance dict, evidence list.
- Repair: `src/validators/knowledge_object_provenance.py`.
- Quality flags: `src/validators/knowledge_object_quality.py` written into `ko.provenance.quality`.
- FirestoreExporter publishes provenance + evidence and does not set conversation_id = object_id.
- Dual UI + Flask still present. Leave until Firestore is the only Studio read path.
- Committed generated `output/` / `uploads/` still inflate the repo if not gitignored on disk.

## Weak / leftover

- Typed Provenance model requires a KnowledgeObject change. Forbidden this pass.
- Compilation quality (titles that are conversation dumps) is an analyzer/compiler problem. Do not fake it in Studio.
- Firestore client still optional-SDK; tests mock it.
- No portable per-object lineage file existed; only relationship edges.

## This pass (additive)

- `ProvenanceLedgerExporter` writes `output/provenance_ledger.json` with platform, file, conversation, messages, attachments, evidence, completeness flags.
- `ExportService` registers the ledger exporter.
- Tests: `tests/test_provenance_ledger_exporter.py`.

## Not done

- Did not delete Flask.
- Did not add vector DB / Docker / second compiler.
- Did not change OKC established code.

## Verify

```text
python -m unittest tests.test_knowledge_object_provenance tests.test_knowledge_object_quality tests.test_provenance_ledger_exporter tests.test_citation_verifier tests.test_firestore_exporter
```
